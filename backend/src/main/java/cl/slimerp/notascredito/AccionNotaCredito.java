package cl.slimerp.notascredito;

// Acciones que quedan registradas en nota_credito_evento (historial append-only).
public enum AccionNotaCredito {
    CREADA,
    EDITADA,
    EMITIDA,
    ANULADA
}
