# Copia este archivo a .env y ajusta los valores antes de levantar docker-compose

DB_NAME=slim_erp
DB_USER=slim_erp
DB_PASSWORD=cambia_esta_clave

# Genera un secreto largo y aleatorio para producción, por ejemplo con:
# openssl rand -base64 48
JWT_SECRET=CAMBIA_ESTE_SECRETO_EN_PRODUCCION_POR_UNO_LARGO_Y_ALEATORIO
